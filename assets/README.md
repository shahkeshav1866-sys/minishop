# Assets

Place your local surprise video at:

- `assets/kiss.mp4`

If this file exists, the app will use the built-in HTML5 video player.
Otherwise, set `CONFIG.optionalEmbedUrl` in `app.js` to a YouTube/Vimeo link.
